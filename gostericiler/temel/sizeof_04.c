#include <stdio.h>

int main()
{
	char*   cp;
	int*		ip;
	double* dp;

	printf("sizeof(cp) = %zu\n", sizeof(cp));
	printf("sizeof(ip) = %zu\n", sizeof(ip));
	printf("sizeof(dp) = %zu\n", sizeof(dp));
}
